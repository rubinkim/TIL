```pytho
# 주요 git 명령어들
git init
git status
git add .
git commit -m <메시지>
git log
git push

# 깃아, remote에 origin이라는 이름으로 url을 add해줘
git remote add origin <url>
# 깃아, origin에 master branch를 push해줘
git push -u origin master
```

